let vehicle = ["Sonata", "Audi", "Fortuner", "Sportage"];
let vehComment = ["is classic sporty", "for exective", "for rough roads", "is a family car"];

for(let i = 0; i < vehicle.length;  i++ )
 {
    console.log(`${vehicle[i]} ${vehComment[i]}`)
 }